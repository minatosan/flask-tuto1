version: '3'

services:
  flask:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - "./flaskblog:/flaskblog/flaskblog"
    tty: true
    #environment:
      #TZ: Asia/Tokyo
    command: python manage.py